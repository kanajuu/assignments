// Adding Data
// arr[3] = "";
// .push();
// .unshift();
// .splice()

// Removing Data
// .pop()
// .shift()
// .splice()
Array.splice(2, 0, "3");
// Array.splice(position, number of items to delete, n number of items to add after the position specified)

// SLICE
// .slice()
// .slice() takes 2 args, works like the spread operator. i.e.:
// Array.prototype.slice(start index, end index(does not include end))
